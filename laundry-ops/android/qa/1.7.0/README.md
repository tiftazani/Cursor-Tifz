# QA Cuciin 1.7.0

Hasil verifikasi 11 September 2026:

- `testDebugUnitTest` dan `testReleaseUnitTest`: 7 tes lulus.
- `lintDebug` dan `lintRelease`: lulus tanpa error/fatal.
- `assembleDebug`, `assembleRelease`, dan `bundleRelease`: lulus.
- APK release: package `com.tiftazani.laundryops`, versionName `1.7.0`, versionCode `10`, target SDK 36, non-debuggable, signature v2, alignment native 16 KB.
- QA emulator offline: beranda, navigasi Service, dropdown Full service/Self-service/Produk, migrasi layanan Cuci mandiri per Load, feedback penambahan, laporan transaksi, interval tanggal/jam, grafik, dan formulir inventory tampil.

Screenshot memakai emulator 1080 × 2400 px. State pengujian lokal dipertahankan; tidak ada data yang dikirim ke server karena mode pesawat aktif.
