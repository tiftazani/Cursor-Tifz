{
  "name": "cuciin-cloud",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply cuciin-db --local",
    "db:remote": "wrangler d1 migrations apply cuciin-db --remote",
    "test": "node --test tests/*.test.mjs",
    "check": "tsc --noEmit && node scripts/validate-schema.mjs && npm test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.1"
  }
}
