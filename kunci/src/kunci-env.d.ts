/**
 * Baked in by vite.config.ts `define`, read from extension/VERSION at build time.
 * Never hardcode a version string in a view: the copy goes stale silently.
 */
declare const __KUNCI_VERSION__: string
